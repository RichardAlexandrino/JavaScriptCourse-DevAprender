// Matching arrays
const numbers = [1,2,3,4,5,6];

const combined = numbers.join('.');
console.log(combined);

const phrase = "Welcome to the class";
const result = phrase.split(' ');
console.log(result);

console.log(result.join('-')); // slog: used for url